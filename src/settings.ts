const settings = {
    themeTransitionDuration: 1000,
    scrollTransitionDuration: 300,
    dottedBackground: {
        dotSize: 4,
        spaceSize: 28,
    },
    initialPadding: 1,
} as const;

export default settings;
